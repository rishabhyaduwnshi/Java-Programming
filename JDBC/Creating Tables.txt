Creating two tables
1. Department
  create table department(dept_no integer primary key not null unique, dept_name text);

2. Students
  create table students(rollNo integer primary key, name text, city text, dept_no integer, foreign key(dept_no) 
                                                                                                        references department(dept_no));
